// 标签
const ROOT_TAG_ID = 'ROOT_TAG_ID' // 顶级标签的id
const DEFAULT_TAG_SORT_ID = 2020 // 默认的排序id
const DEFAULT_TAG_ICON = '📖' // 默认图标

module.exports = {
    ROOT_TAG_ID,
    DEFAULT_TAG_SORT_ID,
    DEFAULT_TAG_ICON
}
